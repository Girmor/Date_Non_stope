# 📸 Функція: Фото-спогади на кожній локації

## 🎯 Мета
Додати можливість робити фото на кожній локації побачення, які потім відображатимуться на фінальному roadmap для спогадів.

---

## 📋 Вимоги від користувача

✅ **Блок перед переходом на наступну локацію** - після підтвердження вибору, перед таймером
✅ **Запитання користувачу** - "Бажаєш зробити фото на згадку?" (або щось подібне)
✅ **Кнопки: Так / Ні** - вибір робити фото чи пропустити
✅ **Відкриття камери** - якщо "Так" → використати getUserMedia API
✅ **Збереження у буфері** - зберегти фото в пам'яті для кожної локації
✅ **Можливість перезняти** - кнопки "Перезняти" / "Ок" після зйомки
✅ **Додавання на roadmap** - фото з'являються на фінальному roadmap справа від картинок локацій (симетрично)

---

## 🎨 UI/UX Дизайн

### 1. Модальне вікно "Фото на згадку"
**Коли показується**: Після натискання "Підтвердити вибір", перед запуском таймера

**Варіанти тексту** (обрати найкращий):
- "Бажаєш зробити фото на згадку?" ⭐ (рекомендований)
- "Зробимо фото тут?"
- "Сфотографувати цей момент?"
- "Зафіксувати спогад?"

**Кнопки**:
- 📸 "Так, зробити фото" (primary button)
- ❌ "Ні, пропустити" (outline button)

**Стиль**: Містичний glassmorphism з пурпурними акцентами

---

### 2. Камера-екран
**Якщо натиснув "Так"**:

**Елементи**:
- 📹 Відео-стрім з камери (повноекранний або картка)
- 🔴 Кнопка "Зробити фото" (великий круглий button)
- ❌ Кнопка "Скасувати" (угорі зліва)
- 🔄 Переключити камеру (фронтальна/тильна) якщо доступно

**Підказка**: "Посміхніться! 😊"

---

### 3. Прев'ю фото
**Після зйомки показати прев'ю**:

**Елементи**:
- 🖼️ Знімок у рамці (містичний стиль)
- ✅ "Ок, зберегти" (primary button)
- 🔄 "Перезняти" (secondary button)

**Дії**:
- "Ок" → зберегти фото в state + запустити таймер
- "Перезняти" → повернутись до камери-екрану

---

## 🏗️ Архітектура і структура коду

### 1. Створити новий клас `PhotoCapture`

**Файл**: `script.js`

```javascript
class PhotoCapture {
  constructor() {
    this.stream = null;
    this.capturedPhoto = null;
    this.videoElement = null;
    this.canvasElement = null;
  }

  // Запитати дозвіл на камеру
  async requestCameraAccess() {
    try {
      this.stream = await navigator.mediaDevices.getUserMedia({
        video: { facingMode: 'user' }, // Фронтальна камера
        audio: false
      });
      return true;
    } catch (error) {
      console.error('Camera access denied:', error);
      Toast.show('Не вдалося отримати доступ до камери 📸', 'error');
      return false;
    }
  }

  // Показати модальне вікно "Бажаєш зробити фото?"
  showPhotoPrompt(stageIndex, onComplete) {
    // Створити модальне вікно з кнопками "Так" / "Ні"
    // onComplete(photoData) - callback з фото або null
  }

  // Відкрити камеру і показати стрім
  openCamera() {
    // Створити video element
    // Показати UI з кнопкою "Зробити фото"
  }

  // Зробити знімок
  capturePhoto() {
    // Намалювати кадр з відео на canvas
    // Отримати dataURL або blob
    // Показати прев'ю
  }

  // Показати прев'ю зі знімком
  showPreview(photoData, onRetake, onConfirm) {
    // Показати зображення
    // Кнопки: "Перезняти" / "Ок"
  }

  // Зупинити камеру
  stopCamera() {
    if (this.stream) {
      this.stream.getTracks().forEach(track => track.stop());
      this.stream = null;
    }
  }

  // Очистити ресурси
  cleanup() {
    this.stopCamera();
    // Видалити DOM елементи
  }
}
```

---

### 2. Інтеграція з StateManager

**Додати до state структури**:

```javascript
state = {
  // ... existing properties
  photos: [
    null, // stage 0 - кава
    null, // stage 1 - захід
    null, // stage 2 - вечеря
    null, // stage 3 - кіно
    null, // stage 4 - подарунок
  ]
}
```

**Методи**:
```javascript
savePhoto(stageIndex, photoDataURL) {
  this.state.photos[stageIndex] = photoDataURL;
  this.save();
}

getPhoto(stageIndex) {
  return this.state.photos[stageIndex];
}

clearPhoto(stageIndex) {
  this.state.photos[stageIndex] = null;
  this.save();
}
```

---

### 3. Інтеграція з MysticStageComponent

**Модифікувати метод handleConfirm()**:

**Було**:
```javascript
handleConfirm() {
  // ... existing code

  // Start timer for next stage
  this.startTimer();
}
```

**Стане**:
```javascript
async handleConfirm() {
  // ... existing code

  // НОВИЙ КОД: Запропонувати зробити фото
  const photoCapture = new PhotoCapture();

  photoCapture.showPhotoPrompt(this.index, (photoData) => {
    // Зберегти фото якщо є
    if (photoData) {
      this.stateManager.savePhoto(this.index, photoData);
      Toast.show('Фото збережено! 📸', 'success');
    }

    // Запустити таймер
    this.startTimer();
  });
}
```

---

### 4. Модифікувати RoadmapGenerator

**Додати відображення фото на roadmap**:

**У методі generate()** після малювання картинки локації:

```javascript
// Намалювати картинку локації (ліворуч)
const img = new Image();
img.src = option.image;
await new Promise((resolve) => {
  img.onload = resolve;
  img.onerror = resolve;
});

const imgSize = 60;
const imgX = pathX - 120; // Ліворуч
const imgY = y - imgSize / 2;

// ... малювання картинки локації

// НОВИЙ КОД: Намалювати фото користувача (праворуч - симетрично)
const userPhoto = this.stateManager.getPhoto(index);
if (userPhoto) {
  const photoImg = new Image();
  photoImg.src = userPhoto;
  await new Promise((resolve) => {
    photoImg.onload = resolve;
    photoImg.onerror = resolve;
  });

  // Розмістити справа симетрично
  const photoX = pathX + 120 - imgSize; // Праворуч
  const photoY = y - imgSize / 2;

  // Малювання круглого фото
  ctx.save();
  ctx.beginPath();
  ctx.arc(photoX + imgSize / 2, photoY + imgSize / 2, imgSize / 2, 0, Math.PI * 2);
  ctx.clip();
  ctx.drawImage(photoImg, photoX, photoY, imgSize, imgSize);
  ctx.restore();

  // Золота рамка навколо фото
  ctx.beginPath();
  ctx.arc(photoX + imgSize / 2, photoY + imgSize / 2, imgSize / 2, 0, Math.PI * 2);
  ctx.strokeStyle = '#ffd700';
  ctx.lineWidth = 3;
  ctx.stroke();
}
```

---

## 🔄 Flow користувача

1. **Користувач обирає локацію** → натискає "Обрати"
2. **Spin animation** → випадкова локація обрана
3. **Натискає "Підтвердити вибір"**
4. ➡️ **НОВИЙ КРОК: Модальне вікно** "Бажаєш зробити фото на згадку?"
   - **Натиснув "Ні"** → пропустити, запустити таймер
   - **Натиснув "Так"** → перейти до кроку 5
5. **Відкривається камера** → відео-стрім
6. **Натискає кнопку "Зробити фото"** 📸
7. **Показується прев'ю** з кнопками "Перезняти" / "Ок"
   - **"Перезняти"** → повернутись до кроку 5
   - **"Ок"** → зберегти фото, запустити таймер
8. **Таймер 15 хв** → очікування
9. **Повторити для всіх 5 локацій**
10. **Фінальний екран** → roadmap з фото користувача справа від локацій

---

## 📋 Checklist для реалізації

### Phase 1: Основний функціонал
- [ ] Створити клас `PhotoCapture`
- [ ] Реалізувати модальне вікно "Бажаєш зробити фото?"
- [ ] Реалізувати відкриття камери через getUserMedia
- [ ] Реалізувати capture фото на canvas
- [ ] Реалізувати прев'ю з кнопками "Перезняти" / "Ок"
- [ ] Додати методи savePhoto/getPhoto до StateManager
- [ ] Інтегрувати з MysticStageComponent
- [ ] Модифікувати RoadmapGenerator для відображення фото

### Phase 2: Стилізація
- [ ] Додати CSS для модального вікна
- [ ] Додати CSS для камери-екрану
- [ ] Додати CSS для прев'ю
- [ ] Додати містичні анімації
- [ ] Адаптивний дизайн для мобільних

### Phase 3: UX покращення
- [ ] Toast повідомлення при збереженні
- [ ] Вібрація при фото
- [ ] Звукові ефекти (опціонально)
- [ ] Обробка помилок (камера недоступна)
- [ ] Loading states
- [ ] Перевірка localStorage розміру

### Phase 4: Тестування
- [ ] Тест на desktop (Chrome, Firefox, Safari)
- [ ] Тест на mobile (iOS Safari, Chrome Android)
- [ ] Тест з різними дозволами
- [ ] Тест без камери (fallback)
- [ ] Тест переповнення localStorage

---

**Статус**: 📋 План готовий до реалізації
**Оцінка часу**: 4-6 годин розробки + 2 години тестування
**Складність**: Medium (потрібно працювати з Camera API)
